# M2.1 — Schema dan raw-event repository

Status: SELESAI pada 2026-09-15. Commit `c27ec8e` dipush ke `task/m2-1-raw-repository`.

PostgreSQL 18.6 dan Psycopg 3.3.5 terverifikasi. Migration idempotent, insert append-only, serta round-trip `timestamptz`, `numeric(0)`, `NULL`, `text[]`, dan `jsonb` lulus pada PostgreSQL lokal. Lima tes konfigurasi M1 juga lulus. Detail implementasi ada di `meme_ai_trader/database/`; tes di `tests/test_raw_events_integration.py`. Deduplikasi dan penanganan event terlambat diserahkan ke M2.2.
