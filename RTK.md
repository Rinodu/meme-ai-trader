# RTK

Gunakan `rtk` untuk command dengan output besar (Git, test, build, pencarian). Gunakan command sempit dan output terkompresi; fallback ke PowerShell/`rg` bila subcommand tidak tersedia. RTK tidak menggantikan navigasi simbol atau validasi yang diperlukan.
